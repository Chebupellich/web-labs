import styles from './editEventStyles.module.scss';

const EditEvent = () => {
    return (
        <div className={styles.wrap}>
            <h2>Edit event</h2>
        </div>
    );
};

export default EditEvent;
